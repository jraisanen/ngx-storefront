export interface Config {
  id: number;
  code: string;
  websiteId: number;
  name: string;
  locale: string;
  currencyCode: string;
  timezone: string;
  weightUnit: string;
  baseUrl: string;
  baseMediaUrl: string;
}
